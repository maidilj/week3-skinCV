"""Model loading + inference. TODO (Phase 4).

Load models/best.pth into the correct architecture, replicate the val/test
transforms, and expose predict(image) -> {"label": str, "probabilities": [...]}.
"""
