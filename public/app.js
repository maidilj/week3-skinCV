// TODO (Phase 6): drag/drop image -> fetch API_BASE + "/predict" -> render
// predicted class + confidence bars + disclaimer. Only JS is fetch to the API.
const API_BASE = "http://localhost:8000"; // configurable: localhost / Docker host / deployed backend
